<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Top Bar with Filename, Delete, Copy, and Close Buttons -->
    <RelativeLayout
        android:id="@+id/top_bar_text_viewer"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="#000000"
        android:elevation="4dp">

        <TextView
            android:id="@+id/file_name_text_viewer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_marginStart="16dp"
            android:layout_toStartOf="@+id/delete_button_viewer"
            android:ellipsize="middle"
            android:singleLine="true"
            android:text="File Name"
            android:textColor="@android:color/white"
            android:textSize="18sp" />

        <ImageButton
            android:id="@+id/delete_button_viewer"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@+id/share_button_viewer"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Delete File"
            android:src="@drawable/delete_forever_24px"
            android:tint="#FFFFFF" />

        <!-- NEW: Share Button -->
        <ImageButton
            android:id="@+id/share_button_viewer"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@+id/copy_button_viewer"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Share File"
            android:src="@drawable/share_24px"
            android:tint="#FFFFFF" />

        <ImageButton
            android:id="@+id/copy_button_viewer"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@+id/close_button_text_viewer"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Copy Content"
            android:src="@drawable/docs_24px"
            android:tint="#FFFFFF" />

        <ImageButton
            android:id="@+id/close_button_text_viewer"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Close Viewer"
            android:src="@drawable/close_24px"
            android:tint="#FFFFFF" />
    </RelativeLayout>

    <!-- Content Display -->
    <ScrollView
        android:id="@+id/content_scroll_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/navigation_bar_viewer"
        android:layout_below="@id/top_bar_text_viewer">

        <TextView
            android:id="@+id/content_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textIsSelectable="true" />
    </ScrollView>

    <!-- Bottom Navigation for Pagination -->
    <RelativeLayout
        android:id="@+id/navigation_bar_viewer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/footer_bar_text_viewer"
        android:background="#000000"
        android:padding="8dp">

        <Button
            android:id="@+id/prev_page_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:background="#4f46e5"
            android:textColor="#FFFFFF"
            android:text="Previous" />

        <TextView
            android:id="@+id/page_number_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="Page 1 / 1"
            android:textColor="@android:color/white"
            android:textSize="16sp" />

        <Button
            android:id="@+id/next_page_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:background="#4f46e5"
            android:textColor="#FFFFFF"
            android:text="Next" />

    </RelativeLayout>

    <!-- NEW FOOTER -->
    <RelativeLayout
        android:id="@+id/footer_bar_text_viewer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#000000"
        android:padding="8dp"
        android:elevation="4dp">

        <LinearLayout
            android:id="@+id/footer_buttons_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/open_with_button_text"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="Open With"
                android:src="@drawable/open_in_new_24px"
                android:tint="#FFFFFF" />

        </LinearLayout>

        <!-- DELETION PROGRESS UI (Initially Hidden) -->
        <RelativeLayout
            android:id="@+id/deletion_progress_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignTop="@id/footer_buttons_layout"
            android:layout_alignBottom="@id/footer_buttons_layout"
            android:background="@android:color/black"
            android:visibility="gone">

            <ProgressBar
                android:id="@+id/deletion_progress_bar"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_marginEnd="8dp"
                android:layout_toStartOf="@+id/deletion_progress_text"
                android:indeterminate="true" />

            <TextView
                android:id="@+id/deletion_progress_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:text="Deleting..."
                android:textColor="@android:color/white" />
        </RelativeLayout>

    </RelativeLayout>

</RelativeLayout>