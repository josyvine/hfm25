<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <!-- MODIFIED: Changed root to a vertical LinearLayout to place filename below the image area -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- This FrameLayout contains the original image and overlays -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="120dp">

            <!-- Thumbnail Image -->
            <ImageView
                android:id="@+id/thumbnail_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:background="#CCCCCC"
                android:contentDescription="File thumbnail" />

            <!-- Index Number -->
            <TextView
                android:id="@+id/index_number"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|start"
                android:layout_margin="4dp"
                android:background="#80000000"
                android:paddingLeft="6dp"
                android:paddingRight="6dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:textColor="@android:color/white"
                android:textSize="12sp"
                android:textStyle="bold"
                android:text="1" />

            <!-- Red Overlay for Excluded Items -->
            <View
                android:id="@+id/exclusion_overlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#66FF0000"
                android:visibility="gone" />

        </FrameLayout>

        <!-- NEW: TextView to display the filename below the image -->
        <!-- It is 'gone' by default and will be made visible by the adapter for non-media files -->
        <TextView
            android:id="@+id/file_name_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="8dp"
            android:gravity="center_horizontal"
            android:textSize="12sp"
            android:textColor="@android:color/black"
            android:maxLines="2"
            android:ellipsize="end"
            android:visibility="gone"
            android:text="filename.txt" />

    </LinearLayout>

</androidx.cardview.widget.CardView>

