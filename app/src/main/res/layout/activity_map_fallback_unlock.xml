<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- The Map View from osmdroid library -->
    <org.osmdroid.views.MapView
        android:id="@+id/map_view_fallback_unlock"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Static Pin/Crosshair in the center of the screen -->
    <ImageView
        android:id="@+id/center_pin_image_unlock"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_centerInParent="true"
        android:src="@android:drawable/ic_menu_mylocation"
        android:contentDescription="Map Center Pin"/>

    <!-- Instruction Text and Status Block -->
    <LinearLayout
        android:id="@+id/instructions_text_map_unlock"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_margin="16dp"
        android:background="#A6000000"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="12dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Place the pin on your secret location to verify and unlock."
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/current_coords_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:fontFamily="monospace"
            android:text="Lat: -, Lon: -"
            android:textColor="@android:color/white"
            android:textSize="12sp"/>

        <TextView
            android:id="@+id/match_status_text_map"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Status: Not Matching"
            android:textColor="@android:color/holo_red_dark"
            android:textSize="14sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- Verify Button at the bottom -->
    <Button
        android:id="@+id/verify_location_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:background="?android:attr/colorPrimary"
        android:text="Verify Location and Unlock"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:enabled="false"/>

</RelativeLayout>

